<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for EzyEnquiry RetailerApp.
  - Debug builds: allow cleartext to local dev IPs (10.0.2.2, localhost, 192.168.*)
  - Release builds: only HTTPS is allowed (no cleartext)
-->
<network-security-config>
    <!-- Production: HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development: allow cleartext to local IPs for Metro and backend -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="true">192.168.1.45</domain>
    </domain-config>
</network-security-config>
